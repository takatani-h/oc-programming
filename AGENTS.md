# Repository Guidelines

## プロジェクト構成

- `main.py`: Pyxel 製ミニカー・タイムアタックの全ゲームロジック。入力、車両挙動、当たり判定、描画を含みます。
- `my_resource.pyxres`: コース、タイルマップ、車両画像などの Pyxel リソース。名前を変える場合は `pyxel.load()` も更新してください。
- `プログラミング操作方法.md`: ワークショップ参加者向けの操作・調整手順。HTML と PDF は配布用の生成物です。
- `pyproject.toml` / `uv.lock`: Python 3.13 以上と Pyxel の依存関係を固定します。

現時点では `tests/` はありません。テストを追加する場合は、対象モジュールに対応する `tests/test_<module>.py` に配置してください。

## ビルド・テスト・開発コマンド

- `uv sync`: ロックファイルどおりに `.venv` へ依存関係を準備します。
- `uv run python main.py`: ゲームをローカル起動します。矢印キーで操作し、`R` でリセットします。
- `uv lock --check`: `pyproject.toml` と `uv.lock` の整合性を確認します。
- `uv run pytest`: テスト導入後に全テストを実行します。pytest を追加するまでは実行できません。

専用のコンパイル工程はありません。変更後はゲームを起動し、スタート、加速、旋回、コースアウト、ゴール、リセットを手動確認してください。

## コーディングスタイルと命名

Python は PEP 8 に従い、4 スペースでインデントします。関数・変数は `snake_case`、定数は `UPPER_SNAKE_CASE` を使います。ゲーム挙動の変更は `update()`、表示の変更は `draw()` にまとめ、座標やタイル番号には意図が分かる名前または短いコメントを付けてください。新しい formatter や依存関係は、必要性を説明できる場合のみ追加します。

## テスト方針

自動テストは未整備のため、変更箇所に応じた手動確認を必須とします。ロジックを分離してテストを追加する場合は pytest を使用し、テスト名は `test_<期待する振る舞い>` とします。画像や操作感に影響する変更では、変更前後のスクリーンショットも残してください。

## コミットと Pull Request

既存履歴に合わせ、コミット件名は日本語の短い命令形または変更内容の要約にします（例: `ゴール判定を修正`）。1 コミットにつき目的を一つに絞ってください。Pull Request には目的、主な変更、確認手順、関連 Issue を記載し、画面表示を変えた場合はスクリーンショットを添付してください。`main.py` と `my_resource.pyxres` の変更は、対応関係が分かるよう同じ PR で説明します。

## リソースと設定の注意

`.pyxres` はバイナリとして扱い、意図しない再保存を避けてください。秘密情報は不要なプロジェクトです。資格情報やローカル固有のパスをソース、文書、リソースへ追加しないでください。
